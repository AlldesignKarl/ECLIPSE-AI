/**
 * La versión de los iconos, para que un icono nuevo llegue a verse.
 *
 * Los archivos se llaman siempre igual —`icon-512.png` y compañía— así que el
 * navegador, y sobre todo el móvil que ya tiene la app en la pantalla de
 * inicio, siguen enseñando el que guardaron el primer día. Colgando este
 * número de la dirección, cada versión es una dirección distinta y no hay nada
 * viejo que reutilizar.
 *
 * Se sube a mano cuando cambien los iconos, y solo entonces.
 */
export const ICONOS = "228";
